
# Lists can contain different data types (integer, float, string, etc)
# Lists are mutable (can be changed) and strings are immutable (cannot be changed) in Python

student = ["karan", 95.4, 17, "Delhi"]
print(student)

str = "hello"
print(str[0])
# str[0] = "y" # Change of elements is not allowed in Python for strings

print(student[0])
student[0] = "arjun" # Elemental change is allowed for lists in Python.
print(student)

